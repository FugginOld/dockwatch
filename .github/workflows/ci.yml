name: CI

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-go@v5
        with:
          go-version: 1.25.0
          cache: true

      - run: go mod download
      - run: go build ./...
      - run: go test ./...
      - run: go vet ./...
